# texpace — Concept Inventory
> One row per spatial/temporal concept: source, dimensions, format support, checkability, and core|module|advisory|out verdict, governed by the checkability filter

Index: this file holds **core** + **module** concepts. **Advisory**, **out-of-scope**, and the
**per-format support matrix** are in [INVENTORY-EXT.md](INVENTORY-EXT.md).

**Source tags:** (a) user brainstorm · (b) M0.5 foundations (`texpace-foundations.md`) · (c) checker-necessity ·
(d) target-format data model · Cx = [CONFLICTS.md](CONFLICTS.md) convention. **Dims:** 2D/2.5D/3D/4D. **Formats:**
SVG · PPTX/OOXML · TMX (Tiled) · glTF 2.0 · IFC · Lottie. **Verdict:** `core` = deterministically checkable +
needed cross-dimension, must name a checker predicate · `module` = checkable but optional/dimension-specific ·
see INVENTORY-EXT.md for `advisory`/`out`.

## Core concepts

| Concept | Source | Dimensions | Formats | Deterministically checkable? (how) | Verdict |
|---|---|---|---|---|---|
| `DIR(fig,gnd,axis,frame)` | b,C1 | 2D/2.5D/3D/4D | all6 | Rectangle Algebra, one Allen relation per axis (base: left/right/above/below/contains/inside/non-overlap), evaluated in canonical +Z-up frame. Grounded: isoroll `ManifestFacing` = `"N"｜"NE"｜"E"｜"SE"｜"S"｜"SW"｜"W"｜"NW"｜"TOP"` (`code/isoroll-module/src/import/manifest-types.ts:4`); slides derives angle from shear/scale (`core/tools/slides_style.py:51-53`) | core |
| `DIST(fig,gnd,θ)` | b | 2D/2.5D/3D/4D | all6 | numeric thresholded distance; ordinal quantities are a type error in DIST (C2). Grounded: corpora `DepthEstimator` wraps DepthAnythingV2 → relative/inverse depth = ordinal, cannot feed DIST (`code/corpora/src/depth.py`) | core |
| `TOP(a,b,rcc)` | b | 2D/2.5D/3D/4D | all6 (native: IFC `IfcRelContainedInSpatialStructure`; derived elsewhere) | RCC-8 {DC,EC,PO,TPP,TPP⁻¹,NTPP,NTPP⁻¹,EQ} region test | core |
| `PATH(fig,gnd,traj,τ)` | b | 2.5D/3D/4D (static-degenerate in 2D) | glTF, Lottie, PPTX (motion-path anim); not TMX/IFC; SVG excluded (SMIL deprecated, unused per format-slate decision) | trajectory sampled at τ + Allen order over waypoints | core |
| Anchor `{world,locale,group}` + arity gate | b | 2D/2.5D/3D/4D | n/a — texpace kernel field, no native store; closest: IFC `IfcAxis2Placement` local/global | checker: `group` without a bound viewpoint → reject as underspecified; `locale` on a non-oriented relatum → reject | core |
| Entity semantic type (intrinsic front / oriented vs unoriented) | b,c | 2D/2.5D/3D/4D | n/a metadata; closest native: IFC `IfcTypeObject`/`PredefinedType` | boolean `hasIntrinsicFront` flag gates locale-anchor validity. Grounded: isoroll tiles carry `ManifestFacing` (oriented — doors, walls); tokens generally don't (`code/isoroll-module/src/import/manifest-types.ts`) | core |
| Temporal `AT(t)` / `OVER(interval)` | b | 4D | glTF, Lottie, PPTX (click/timing); not TMX/IFC/SVG | Allen point-vs-interval containment | core |
| Composition `SEQ`/`PAR`/`ALT` | b | 4D | glTF (tracks), Lottie, PPTX (`<p:timing>`); not TMX/SVG/IFC | Allen relations: before/meets/overlaps→SEQ, equals→PAR; ALT is an authoring-only acceptance set, not geometric | core |
| Quantifiers `REPEAT(action,N)` / `HOLD(pred,Δ)` | b | 4D | glTF (loop flag, runtime), Lottie (native loop/time-remap), PPTX (repeat count), TMX (tile-anim frame duration) | count/duration check against declared timebase; `HOLD` on an ordinal (`steps`) timebase is a type error (C3) | core |
| Units & scale (unit-tagged quantity) | c,C2 | 2D/2.5D/3D/4D | all6, native unit kind differs (metric: IFC/glTF metres; device: SVG px/PPTX EMU/Lottie px; grid: TMX cells) | checker: unit-kind type check; ordinal quantities rejected from metric predicates. Grounded: isoroll voxel=1.5m declared vs `DEFAULT_WALL_H` 3-vs-2 divergence (C2) | core |
| Tolerance / epsilon | c | 2D/2.5D/3D/4D | n/a — texpace-declared, applies regardless of format | every numeric predicate carries an explicit ε; comparisons are `|Δ|<ε`, never exact equality. Grounded: isoroll `EPS` (`code/isoroll-module/src/transform/tile-transform.d.ts`); tolerances IoU≥0.9/DIM_TOLERANCE=0.02/ALPHA_MIN=8 per spacemantics ROADMAP.md Key Files (sibling repo `isoroll-content/src/cli/sheet_qc.py`, not present in this checkout) | core |
| Frame declaration (handedness, up-axis, origin) | c,C1 | 2D/2.5D/3D/4D | all6, each a fixed native convention (SVG/PPTX/Lottie Y-down top-left; TMX Y-down; glTF Y-up RH; IFC Z-up RH) | adapter is a declared bijection to canonical (+Z-up RH); mismatched declaration → parse error | core |
| Coordinate systems (local vs global / nested transforms) | a | 2D/2.5D/3D/4D | SVG (`<g transform>`), PPTX (group `xfrm`+`chOff/chExt`), glTF (node hierarchy), IFC (`IfcLocalPlacement` chain), Lottie (precomps); TMX is flat-only (no nesting) | transform-chain composition test: `child_world = parent_world ∘ local`. Grounded: isoroll six coord systems WORLD/VIEWPORT/SCREEN/IMAGE/GRID/ISO3D, hub-and-spoke via `transformCoord()` (`code/isoroll-module/src/transform/coord-map.ts`) | core |
| Negation & quantification (∀/∃ over base predicates) | c | 2D/2.5D/3D/4D | n/a kernel logic layer | universal/existential closure over the O(n²) pairwise predicate matrix, e.g. "no two overlap" = ∀ pairs `TOP∉{PO,EQ,TPP,NTPP}` | core |
| Underspecification (which DOFs are free) | c | 2D/2.5D/3D/4D | n/a authoring-time | DOF-count over the declared constraint system; *which* DOFs are free is decidable even when their values aren't | core |
| Constraints-vs-assertions (goal-to-solve vs fact-to-verify) | c | 2D/2.5D/3D/4D | n/a kernel eval-mode flag | checker dispatches by mode: assertion → boolean predicate eval; constraint → tiered solve (Cassowary → geometric QC → Z3, foundations §4) | core |
| Parametric variables + spatial arrays w/ per-instance overrides | c,C5 | 2D/2.5D/3D/4D | IFC (native occurrence pattern), TMX (tileset repetition, partial), glTF (`EXT_mesh_gpu_instancing`, extension not core spec), SVG (`<use>`+transform, no override schema); PPTX/Lottie none — must flatten on emit | instance count matches array spec; override map resolves against the base module without contradiction. Grounded: casinhas 7× module array + corner-unit override, planned `modelo/build_ifc.py` (ifcopenshell script-first) + `mapeamento_ifc.csv` selector schema `regra_id,seletor_tipo,seletor,item_id,qto_atributo,fator` (`branches/casinhas/ROADMAP.md`) | core |
| Selectors / identity (stable derived identity, e.g. `houses[3]`) | c | 2D/2.5D/3D/4D | all6 (id/name attrs); IFC `GlobalId` is strongest (stable UUID) | selector resolves to exactly one entity (uniqueness), else errors | core |
| Geometric primitives (points/vectors/lines/curves/shapes/AABB/volume boxes) | a | 2D/2.5D/3D/4D | all6 natively | trivial: min/max extraction, curve sampling at declared resolution | core |
| Orientation/rotation (canonical unit quaternion) + pivots/rotation axes | a,C4 | 2D/2.5D/3D/4D | glTF (native quaternion); SVG/PPTX/TMX/Lottie 2D angle/flip only; IFC direction cosines — all need conversion except glTF | angle-between-axes via dot/cross product on the normalized quaternion; never round-trips through Euler degrees. Grounded: isoroll projection presets (`PROJECTION_TYPES`, `DIMETRIC_2_1`, `code/isoroll-module/src/transform/constants.ts`) are camera descriptors, NOT entity orientation (C4 corollary) | core |
| Schematization (ground keeps axes+region, figure collapses to a point) | b | 2D/2.5D/3D/4D | n/a modeling rule, format-agnostic | not independently asserted; enforced structurally by the DIR/TOP evaluator's figure/ground role assignment — verified indirectly via DIR/TOP consistency | core |

## Module concepts

| Concept | Source | Dimensions | Formats | Deterministically checkable? (how) | Verdict |
|---|---|---|---|---|---|
| Viewpoint / camera descriptor (FOV, near/far, projection type) | a,b | 2.5D/3D | glTF (native camera node); IFC weak/presentation-only; SVG/PPTX/TMX/Lottie have no native camera (implicit ortho viewer) | camera fully declared (position+orientation+FOV+clip) before any `group`-anchor statement is evaluable | module |
| Projection presets (perspective/orthographic/isometric/compass) | a,C4 | 2.5D/3D | TMX (native `orientation` field), glTF (`camera.type`); SVG/PPTX/IFC/Lottie have no true projection — "isometric" is faked geometry | preset is a fixed camera/projection descriptor, forbidden from an entity's transform (C4 corollary) | module |
| Visibility / line-of-sight (occlusion, fog-of-war) | a | 2.5D/3D | no target-format native equivalent; render-time concept | ray-cast / point-visibility boolean test against occluder topology. Grounded: isoroll fog states — default visible / `EXPLORED_TINT=0x808080` / unseen, `testPointVisible()` (`code/isoroll-module/src/render/fog-state.ts`) | module |
| Z-order / depth / paint order (component ordering) | a,C6 | 2.5D | SVG/PPTX/Lottie authored (document/layer order); TMX native layer stack + tile gid; glTF has no native paint order (render-time) | depth is *derived*, never authored — `depth=dot(position,view_dir)` + tie-break band. Grounded: isoroll's 3 disagreeing formulas — `(row−col+elev)*10000+band` live vs `x+y+elev` dormant/occluder — are the same formula under different cameras (`code/isoroll-module/src/render/iso-tile-depth.d.ts`, CONFLICTS.md C6) | module |
| Layers (active/inactive, visible/hidden, layer-tagging) | a | 2D/2.5D/3D/4D | TMX (native `visible` flag), Lottie (native `hd`), IFC (native `IfcPresentationLayerAssignment`); PPTX has no true named layers; SVG is convention not spec; glTF has no core-spec toggle | boolean visible/active flag per entity, independent of z-order. Grounded: isoroll `LayerManager.ensureLayer/bringToTop/clearLayer/declareOrder` (`code/isoroll-module/src/render/layer-manager.ts`) | module |
| Grouping / scene-canvas hierarchy | a | 2D/2.5D/3D/4D | all6 native (SVG `<g>`, PPTX `<grpSp>`, TMX object groups, glTF node children, IFC spatial structure, Lottie precomps) | tree well-formedness (no cycles) + transform composition | module |
| Timeline: checkpoints / keyframes / transitions | b | 4D | glTF (`animation.channels/samplers`), Lottie (native `k` keyframe arrays), PPTX (`<p:timing>` tree); TMX tile-anim frames only; SVG unsupported (SMIL unused); IFC none | keyframe sequence obeys declared timebase + Allen ordering between checkpoints | module |
| Dynamics `{rate,onset,straightness}` / easing-interpolation | b,c | 4D | glTF (sampler LINEAR/STEP/CUBICSPLINE), Lottie (bezier easing handles), PPTX (native accel/decel presets); TMX/SVG/IFC none | `value(t)` is a declared deterministic function; rate/onset/straightness measured as numeric derivatives at threshold — a separate execution layer, never the scored relation itself (design principle 7) | module |
| Symmetry / mirroring / chirality | c,C4 | 2D/2.5D/3D | all6 support the transform (negative-scale or explicit flip flags: PPTX `flipH/flipV`, TMX gid flip bits; glTF/IFC discourage negative-determinant) | sign of the transform determinant classifies remap-vs-mirror; texpace's chirality rule = rotation is cell-remapping, never mirroring. Grounded: isoroll `coord-sys-image.ts` uses `Math.abs(scale.x)` (ignores flip sign) — roundtrip self-consistent but UV mirrors for flipped tiles (`code/isoroll-module/src/transform/CONTEXT.md` Key Gotchas) | module |
| Clearance / min-gap | c | 2D/2.5D/3D | n/a native field in any format; derived wherever DIST works | thresholded DIST between nearest surfaces, not just centroids | module |
| Snapping / grid | c | 2D/2.5D | TMX (native — the format IS a grid), IFC (`IfcGrid` structural axes, partial); SVG/PPTX editor-only (not file-native); glTF/Lottie none | position ≡ grid-cell multiple within ε | module |
| Clipping & near/far planes | c | 2.5D/3D | glTF (camera `znear/zfar` native), SVG (`clipPath`, no near/far), PPTX (crop, no near/far); TMX/IFC/Lottie none (masks ≠ clip planes) | point-in-frustum numeric test | module |
| Viewport / aspect / DPI | c | 2D/2.5D | SVG (`viewBox`+`preserveAspectRatio`), PPTX (slide size+EMU), TMX (map pixel dims), Lottie (`w/h`); glTF/IFC none (consumer concern) | required to resolve `device`-kind units (C2) to metric; missing declaration → device units stay untyped | module |
| Cross-track event ordering (across independent timelines) | b,c | 4D | PPTX (`<p:timing>` sequence, native), Lottie (precomp time-remap, semi-native), glTF (independent tracks, app-level order only); TMX/SVG/IFC none | Allen relations evaluated *between* tracks' event intervals, not just within one — extends SEQ/PAR/ALT to multi-track scope | module |
